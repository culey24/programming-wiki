OPENROUTER_API_KEY=
JWT_SECRET=
# Optional — defaults to google/gemini-2.5-flash
# OPENROUTER_MODEL=google/gemini-2.5-flash

# Docker Desktop (Windows / if PWD is wrong): absolute path to repo .run-sandbox
# EXEC_SANDBOX_HOST_PATH=/full/path/to/programming-wiki/.run-sandbox
